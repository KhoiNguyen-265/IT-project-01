<h2>
    <a href="{{ $href }}"
        class=" text-white font-bold mb-6 bg-blue-400 p-3 rounded flex items-center gap-2">
        @if($icon)
        <i class="{{ $icon }}"></i>
        @endif

        <span>{{ $title }}</span>
    </a>
</h2>
